# Codebase Assessment: ai-native-toolkit

_Generated 2026-06-01. Generated by `/assess` v1.23.0._

**Score: 6.0 / 8 - Solid** · Keyhole: 6 structural concerns (6 hidden coupling), 1 safe zone.

> This is an improvement roadmap, not a verdict - a **Missing** or **Partial** locates where someone working here is partly blind, not a mark against the code.

## Top 3 Actions

The mandatory attention paths (the `hidden_coupling` seams) are covered by Actions 1-2; Action 3 addresses the single highest-leverage scorecard gap (L0 navigability), which moves the overall score from 6.0 toward 7.0.

| # | Action | Layer | Effort | Command / First Step | Hotspot files this addresses | Issue |
|---|--------|-------|--------|---------------------|------------------------------|-------|
| 1 | Document the `assess_core` -> `scripts/lib` seam so the coupling is owned, not just observed - confirm it's intentional cohesion, not accidental entanglement. Treat as a **strangler-fig boundary** only if the core is later split; do not rewrite working code. | 0/1 | medium | Add `skills/assess/scripts/lib/README.md` describing each lib module's responsibility and why they co-change | `skills/assess/scripts/assess_core.py`, `skills/assess/scripts/lib/doc_graph.py`, `skills/assess/scripts/lib/keyhole_signals.py` | #110 |
| 2 | Document the test↔source co-change seam for `scripts/tests` + `skills/assess/tests` so reviewers know the suites are expected to move with the engine; keep tests co-located but note the dependency. | 4 | small | Add a note to `skills/assess/tests/` on the source files each suite pins | `skills/assess/tests/test_assess_core.py`, `skills/assess/tests/test_doc_graph.py` | #111 |
| 3 | Add a Map-of-Content / index doc and link the orphaned `agents/`, `commands/`, `skills/`, and `docs/` trees from `README.md`; wire the 22 named-but-unlinked cross-references. Moves L0 from Partial to Present. | 0 | medium | Create `docs/index.md` (or extend README) linking each subtree's entry points; add per-module base docs (currently 1 of 8 module dirs has one) | `README.md`, `docs/` (39 islands), `agents/`, `commands/` | #112 |

### Why these three?

Actions 1 and 2 are the deterministic attention list - they make the only structural concern (hidden coupling inside the `/assess` engine) *owned and documented* rather than silently relied upon, which is the correct response when the coupling is genuine cohesion rather than a bug. Action 3 is the single highest-leverage scorecard move: L0 navigability is the only read-side gap that is fixable (L1 and L6 absences are honest and structural), and a Map-of-Content closes the 90% orphan rate that otherwise forces every agent to directory-walk instead of following a curated trail.

## Snapshots

### Complexity - riskiest to change

[![Complexity hotspot](./complexity-heatmap.svg)](./complexity-heatmap.svg)

Every hotspot is in `skills/assess/` - the assessment engine is auditing itself, and its own deterministic core is the most complex, most-churned surface in the repo; per-function ccn p95 9 (against a 15 gate) confirms the high file-aggregate numbers are sums of many small functions.

### Doc navigability - can an agent find its way?

[![Doc map](./doc-graph.svg)](./doc-graph.svg)

About 10% link-reachable from the entry points - a wayfinding/curation gap (an agent can `ls` and open any file by path), not hidden content; the graph is clean (zero dangling/broken links), so there is no lying map.

<details>
<summary>📈 Snapshot detail (commit, hotspots, navigability, lying maps)</summary>

#### Complexity profile

- **Measured at commit:** `5a6eb80906f8` (2026-06-01) - **working tree had uncommitted changes; figures include un-committed edits** (the `.assess/` run artefacts written into this worktree)
- **Files scored:** 69
- **Churn window chosen:** last 12mo
- **Complexity profile:** per-function ccn p95 9 (max 38, across 957 functions); file-aggregate ccn p95 111 (max 169); p95 LOC 489 (max 790)
- **Top hotspots** (composite `sqrt(ccn) × sqrt(1 + commits)` - a complex-AND-active file leads, a frozen-but-complex file ranks below it, a churny-but-trivial file can't top the list on churn alone). `ccn` here is the **file aggregate**; the worst single function per file is in parentheses:
  1. `skills/assess/scripts/assess_core.py` - 497 LOC, aggregate ccn 106 (worst function 38), 15 commits in window
  2. `skills/assess/scripts/lib/doc_graph.py` - 514 LOC, aggregate ccn 169 (worst function 23), 7 commits in window
  3. `skills/assess/scripts/complexity-treemap.py` - 482 LOC, aggregate ccn 115 (worst function 19), 10 commits in window

Size encodes lines of code, colour encodes cyclomatic complexity (dark red = high), saturation encodes recent git churn (vivid = active). Vivid red blocks are the migration risk. Note the shape of the risk here: every hotspot is in `skills/assess/` - the assessment engine is auditing itself, and its own deterministic core is the most complex, most-churned surface in the repo. The per-function ccn p95 of 9 (against a `max-complexity=15` gate) confirms the high *file-aggregate* numbers are sums of many small functions, not a few monster functions - the 169 on `doc_graph.py` is 514 lines of mostly-simple graph code whose worst single function is ccn 23.

#### Doc navigability

Of **41 docs**, only about **10% are reachable** by following links from the entry points (`README.md`, `CLAUDE.md`). The other ~37 are **orphans** (nothing links to them) or sit in one of **39 disconnected islands** - among them every `agents/*.md` (e.g. `agents/assess-layer-scorer.md`), every `commands/*.md`, all 8 `SKILL.md` files, and the whole `docs/superpowers/plans/` tree. The link-graph has just **2 edges** across the entire doc set.

**Frame this as curation, not access.** An agent can still `ls docs/` and open any file by path - nothing is hidden. What is missing is a *navigable map*: there is no index / Map-of-Content knitting the trees together, so an agent forms its picture by directory-walking rather than by following a curated trail. The fix is a wayfinding improvement (add an index/MOC, wire the cross-references), and because the repo is already cleanly directory-organised, this is a **medium-priority enhancement, not a blocker**.

The doc graph is **clean** - zero dangling links, zero broken links - so this is a *wayfinding* gap, not a *truth* gap. There is **no lying map** here.

**On the "stale hub" signal:** the top staleness candidates (`skills/deslop/references/full-checklist.md`, `.github/claude-review-instructions.md`, `agents/assess-layer-scorer.md`) all show an inflated churn-ratio of 199, but every one carries `confidence: low` because its subject-association is `repo-baseline` (the "subject churn" is just the whole repo's churn, a coarse proxy) - and all three were committed **0-3 days ago**. They are freshly-edited docs, not frozen maps. This signal is correctly *not* flagged as a lying map.

Colour = staleness (vivid red = a frozen doc beside churning code); structure = reachability (centre = entry, rim = unreachable, dashed ring = orphan); size = file length. Open the SVG directly for per-node hover tooltips.

#### What changed since last run

_Diff suppressed - the prior snapshot was produced by plugin v1.19.2 (current is v1.23.0), and file-filter differences across versions surface phantom graduated/new transitions that didn't really happen. Cross-run comparison resumes once two runs share a plugin version._

</details>

<details>
<summary>📊 Full scorecard (per-layer evidence & gaps)</summary>

The two headline metrics measure **different things and are never combined.** The 0-8 score answers _"is the scaffolding in place to catch problems?"_ The Keyhole Readiness summary answers _"where is today's structural pain?"_ - a count of structural concerns and safe zones from the eight cross-layer findings. Here: strong scaffolding (6.0/8) over a small, cohesive, actively-developed subsystem whose files change together (the 6 hidden-coupling concerns).

The **What it asks** column is the question that layer answers for an agent working here - read it first; the framework name is secondary. The **Band** orders layers by dependency.

| Layer | What it asks | Band | Status | Evidence | Gap |
|-------|--------------|------|--------|----------|-----|
| 0: Agent Instructions & Navigability | Can I build a true map of this codebase before I touch it? | read | **Partial** | Instructions are A-grade: `CLAUDE.md` (A-, 184 lines, 26 positive directives), `.github/claude-review-instructions.md` (A, 237 lines), 8 skills give genuine progressive disclosure, no broken refs, nothing untracked. | Navigability is weak: 41 docs / 2 link edges, 90% orphan rate, ~10% reachable; `large_repo` with 1 of 8 module dirs carrying a base doc; 22 named-but-unlinked cross-references. Add an index/MOC. |
| 1: Runtime Legibility / Liveness | Can I see which parts are live, which need attention, which are dead weight? | read | **Partial** | Dead-code scan ran clean (vulture, 0 candidates). With no deployed runtime, liveness is read through complexity + churn + reachability, not telemetry. | Observability rung 0 (`instrumented: false`) - but there is no application runtime to instrument, so this is structurally unknowable, not neglected. Honest gap. |
| 2: Code Design | Will the type-checker catch my mistakes? | write | **Present** | `mypy` runs blocking in CI over `scripts/lib`; type hints made a contract (issue #79). | Type scope could widen beyond `scripts/lib` (documented ratchet plan). |
| 3: Linters | Are complexity and style bounds enforced, or will my code drift? | write | **Present** | ruff `C901` mccabe gate `max-complexity=15` enforced in CI across `skills/assess` + `scripts`. Per-function p95 = 9; the 4 functions over the line carry explicit `# noqa: C901` - an honest, named ratchet. | - |
| 4: Architecture Tests | Are the structural conventions executable, or just folklore? | write | **Present** | `tests/test_plugin_contract.py` enforces structure as a blocking CI contract: SKILL frontmatter present, `TRIGGER` clause required, internal markdown links resolve, marketplace entries exist on disk, no placeholder tokens. | - |
| 5: CI Pipeline | Does something automatically catch a bad change before it merges? | write | **Present** | 4 workflows; `tests.yml` runs 3 pytest jobs + ruff/mypy on every PR/push. Branch protection with `enforce_admins: true` and 4 required checks blocks merge. | - |
| 6: Coverage Gates | Do the tests constrain behaviour, or just execute lines? | write | **Missing** | No codecov/`.coveragerc`/threshold config, no `cov` in CI, no mutation config. | Honest, documented absence (CLAUDE.md "What this repo doesn't have"). The mutation harness already exists in `lib/test_pressure/` (opt-in) if behavioural pinning is ever wanted. Low priority. |
| 7: Code Review Bots | Is there design-level feedback on every change? | write | **Present** | `claude-review.yml` runs `anthropics/claude-code-action@v1` (Opus) on every PR with a 237-line A-graded instruction file; advisory (`continue-on-error`) by design. CodeRabbit also configured. | - |
| 8: AI Project Mgmt (capstone) | Do learnings feed back into the contracts, or evaporate? | meta | **Present** | Task Master + marathon skill for orchestration; `Retrospective` + `marathon-retros.md` feedback loop; plugin.json version-bump-per-PR discipline. This self-assessment is itself a feedback cycle. | - |

**Layer 1 caveat:** Layer 1 scores what the *repo* makes agent-reachable, not what the agent has installed at runtime. Here there is no deployed runtime at all, so the read-side instruments (heatmap, churn, dead-code scan) *are* the observability - and they report a healthy, well-exercised codebase.

### Score derivation (worked)

Present = 1, Partial = 0.5, Missing = 0. Raw sum = 0.5 (L0) + 0.5 (L1) + 1 (L2) + 1 (L3) + 1 (L4) + 1 (L5) + 0 (L6) + 1 (L7) + 1 (L8) = **6.0**. Below the cap of 8, so the displayed score is **6.0 / 8**.

### Maturity Level

| Score | Level | Description |
|-------|-------|-------------|
| 0-2 | Not Ready | Agent will produce inconsistent, unvalidated code |
| 3-4 | Basic | Norms exist but aren't enforced. Agent works but drifts |
| 5-6 | **Solid** | Contracts catch most issues. Agent is productive |
| 7-8 | AI-Native | System self-improves. Agents work reliably at scale |

At **6.0**, this repo sits at the top of the **Solid** band - the entire write-side (L2-L5, L7) and the meta capstone (L8) are genuinely enforced via branch protection. The two half-points and the one zero are all on the read side, and all three are either an honest structural absence (L1, L6) or a wayfinding-curation gap (L0) rather than a missing safety net.

</details>

<details>
<summary>🔎 Cross-layer findings & lying signals (keyhole detail)</summary>

### Lying Signals

The most dangerous failure mode is an artefact that looks true but isn't. This run produced **none** - the doc graph is clean (zero dangling/broken links), the stale-hub candidates are all freshly-edited `repo-baseline`/`confidence: low` proxies, and no mutation data was collected to flag a green-but-hollow gate.

## Cross-Layer Findings (Keyhole Readiness)

These are the axis-crossing signals no single layer surfaces - where the static structure and the git history disagree. The dominant signal here is benign-by-context: the assessment engine's scripts and their tests change together because they *are* one cohesive, actively-developed subsystem.

### hidden_coupling

Action: investigate the seam

Paths:
- scripts
- scripts/tests
- skills/assess/scripts
- skills/assess/scripts/lib
- skills/assess/tests
- skills/assess/tests/fixtures

### refactor_boundary

Action: safe to hand an agent in isolation

Paths:
- commands

### Attention List (Priority Order)

- scripts (score 1): hidden_coupling
- scripts/tests (score 1): hidden_coupling
- skills/assess/scripts (score 1): hidden_coupling
- skills/assess/scripts/lib (score 1): hidden_coupling
- skills/assess/tests (score 1): hidden_coupling

**Reading these:** every concern is `hidden_coupling` within the `/assess` engine - `scripts/lib` modules and their `tests/` move in the same commits. For a tightly-cohesive subsystem under active development this is *expected*, not a defect: the seam is real but the coupling reflects genuine shared behaviour, not accidental entanglement. The one **safe zone** is `commands/` (containment 0.92) - edits there stay local, so it is the directory you can hand an agent in isolation with the least risk.

</details>

<details>
<summary>✅ Strengths & further opportunities</summary>

### Strengths

- **The entire write-side is genuinely enforced, not cosmetic.** L2 (mypy), L3 (ruff C901), L4 (`test_plugin_contract.py`), L5 (CI) and L7 (Claude review) all gate merge via branch protection with `enforce_admins: true` - even the maintainer cannot bypass the checks.
- **The ruff complexity ratchet is exemplary.** A `max-complexity=15` gate with explicit, named `# noqa: C901` escape hatches on the 4 known offenders is the textbook honest ratchet: it holds the line on new code while consciously owning the existing exceptions rather than hiding them.
- **Architecture conventions are executable.** `tests/test_plugin_contract.py` turns "every skill has a TRIGGER clause, every internal link resolves, every marketplace entry exists on disk" from folklore into a blocking CI contract - exactly the convention-as-contract pattern this report rewards.
- **The feedback loop closes (L8).** Task Master, the marathon skill, `marathon-retros.md`, and per-PR plugin.json version bumps mean learnings feed back into the contracts. This very self-assessment is part of that loop.
- **`commands/` is a clean refactor boundary** (containment 0.92) - a safe zone to hand an agent in isolation.

### Additional Opportunities

- **L6 (optional):** if behavioural pinning is ever wanted, wire the existing opt-in mutation harness in `skills/assess/scripts/lib/test_pressure/` into a CI job - but only if the explicit "no coverage gates" decision in CLAUDE.md is revisited. Not a refactor task.
- **L0 symlinks:** CLAUDE.md notes `AGENTS.md` / `GEMINI.md` should be symlinked to `CLAUDE.md` for cross-tool coverage; not yet done (minor).
- **Contradiction sweep (out of deterministic scope):** the `docs/superpowers/plans/` tree is the largest orphan cluster - worth running an LLM pass over it to flag any plan that contradicts shipped behaviour.
- **L2 ratchet:** widen mypy scope beyond `scripts/lib` per the documented plan.

**If you are an agent working in this repo, read the `.assess/` directory - it is actionable feedback written for you, not just a report you skim once.**

- `.assess/assess-report.md` - this report: the scorecard, the findings, and the Top 3 Actions with exact commands and file paths.
- `.assess/hotspots/<file>.md` - a per-file briefing for each hotspot, with a **Suggested actions** section. Read it before changing a file that appears there.
- `.assess/index.md` - the catalog of every hotspot ever flagged (current and graduated).
- `.assess/log.md` - run history, so you can see whether a hotspot is regressing, persistent, or improving.

</details>

<details>
<summary>🧭 How to read this report (framing & method)</summary>

This is an improvement roadmap, not a verdict. It measures one thing: **is the codebase kept honest, not just scaffolded.** It pairs three views:

- **Where the codebase is today** - the complexity heatmap shows current complexity and churn. Vivid red = complex AND actively changing = the files most likely to bite an agent (or a human) next week.
- **Whether an agent can navigate it** - the doc graph shows the docs' link structure: how much is reachable from the entry point, and which docs are stale maps of churning code.
- **What keeps it from getting worse** - the AI Readiness score (0-8) across three bands: read-side foundation (can the agent form a true picture?), write-side enforcement (can it be trusted to produce good output?), and meta (does the system keep itself honest over time?).

A codebase can be 8/8 and still on fire (great scaffolding, legacy debt) - or 2/8 with a calm treemap (small codebase, no enforcement needed yet). The views matter together.

**Each layer is a sense the agent needs, not a box to tick.** Every row in the scorecard names one thing an agent (or a newly-arrived human) must be able to *see or trust* before working safely here; it's phrased as the question that row answers. A **Missing** or **Partial** is not a mark against the codebase - it locates where someone working here is partly blind, and the Gap column says what would restore the view. **Layer 1 (Runtime Legibility) is the agent's attention-sense** - a running service answers it through telemetry, a repo with no deployed runtime answers it through complexity, churn, and reachability, so the heatmap and the dead-code scan *are* its observability.

**The legacy-transition lens - this is not a new problem.** The hard part of AI-assisted coding is the same one the industry has faced for 25 years: *what happens when the people who understood the code are gone, or the code outruns anyone's ability to understand it?* AI only changes the **velocity**. So read this report through the accumulated discipline of legacy-code engineering, not as a novel AI concern.

- **"Legacy code is code without tests."** - Michael Feathers (2004). A file that is complex, churning, and untested is already legacy. The response is **characterization tests** (pin actual current behaviour) plus a **seam** to get it under test, *then* refactor.
- **Don't rewrite - strangle.** A rewrite discards the undocumented behaviour a working system has accreted (Joel Spolsky, 2000). Prefer Martin Fowler's **Strangler Fig** - replace incrementally behind tests.
- **Hotspots and change-coupling are the map.** The complexity-×-churn heatmap and the coupling findings here *are* Adam Tornhill's (CodeScene) hotspot and change-coupling analyses. Treat the vivid-red hotspots and the `hidden_coupling` seams as the prioritized worklist.
- **Decide per component (the 7 Rs).** Not every flagged unit should be refactored: retain, retire, rehost, replatform, refactor, re-architect, rebuild.

**How it's measured.** This is an AI-readiness review run almost entirely on *traditional* tooling - static analysis, git history, and graph metrics over the docs and code. The model only writes the prose around those numbers; it does no scanning itself. That keeps a full run fast and close to zero in model tokens, and makes the structural findings reproducible run-to-run.

</details>

<details>
<summary>🤖 Machine-readable data (for agents)</summary>

Structured sidecars an agent can ingest directly, without re-parsing this prose:

- `.assess/run-context.json` - the full data bus (findings, attention, keyhole summary, prescribed actions, stats, diff).
- `.assess/complexity-stats.json` - complexity percentiles plus the ranked file lists (`top_hotspots`, `top_complex`, `top_large`).
- `.assess/hotspots/<file>.md` - per-file briefings, each with a **Suggested actions** section.
- `.assess/index.md` - the catalog of every hotspot ever flagged (current and graduated).
- `.assess/log.md` - append-only run history.

</details>

---

_Report generated by [`/ai-native-toolkit:assess`](https://github.com/bjcoombs/ai-native-toolkit). Install in any Claude Code session: `/plugin marketplace add https://github.com/bjcoombs/ai-native-toolkit` then `/plugin install ai-native-toolkit@ai-native-toolkit`._
